{
  "comment": "Example Claude Desktop configuration for xtool MCP Server",
  "comment2": "Run './run-server.sh -c' to get the exact configuration for your system",
  "comment3": "For platform-specific examples, see the examples/ directory",
  "mcpServers": {
    "zen": {
      "command": "/path/to/xtool_mcp_server/xtool_venv/bin/python",
      "args": ["/path/to/xtool_mcp_server/server.py"]
    }
  }
}